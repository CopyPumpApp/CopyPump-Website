{
  "name": "copypump-site",
  "private": true,
  "version": "0.1.0-alpha",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:web": "vite",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "vite build && node scripts/header-build-audit.mjs",
    "preview": "vite preview",
    "start": "vite preview --host 0.0.0.0 --port ${PORT:-8787}",
    "check:clean": "node scripts/clean-public-audit.mjs",
    "check:ui": "node scripts/ui-regression-audit.mjs",
    "test:e2e": "playwright test",
    "check": "npm run typecheck && npm run check:clean && npm run check:ui && npm run check:agent && npm run build",
    "check:agent": "node --check scripts/kimi-web.mjs && node --check scripts/discover-web-radar.mjs && node --check scripts/refresh-radar.mjs && node --check scripts/radar-data-guard.mjs"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0",
    "@types/node": "^22.0.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^6.0.0",
    "typescript": "^5.7.0",
    "vite": "^8.0.5"
  },
  "engines": {
    "node": "^22.13.0 || >=24.0.0"
  }
}
